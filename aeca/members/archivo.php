<?php 
/*$segundos = 300; //si pasa este tiempo se detecta la inactividad del usuario en el sitio 
if(($_SESSION['tiempo']+ $segundos) < time()) { 

session_destroy();
/*unset($_SESSION['member']); 
// you may want to delete the session cookie 
if (isset($_COOKIE[session_name('member')])) { 
setcookie(session_name('member'), '', time()); 
} 
 // destruyo la sesión*/
 
 
 ?>
  <!-- <script type="text/javascript">alert("Su sesion ha expirado por inactividad, vuelva a logearse para continuar");
   window.location.href="login.php";
   </script>-->
   <?php    
//}else 
  // $_SESSION['tiempo']=time(); 
?>